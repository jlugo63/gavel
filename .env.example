# ─── Gavel Governance Gateway ─────────────────────────────────
# Bearer token required for POST /approve (human approval flow)
HUMAN_API_KEY=change-me-before-production

# ─── PostgreSQL (docker-compose) ──────────────────────────────
POSTGRES_USER=admin
POSTGRES_PASSWORD=password123
POSTGRES_DB=governance_control_plane

# ─── Database URL (used by gateway + dashboard) ──────────────
DATABASE_URL=postgresql://admin:password123@localhost:5433/governance_control_plane

# ─── Dashboard (ui/.env.local) ────────────────────────────────
# GATEWAY_URL=http://localhost:8000

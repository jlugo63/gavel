# ─── Gavel Governance Gateway ─────────────────────────────────
# API keys are now per-operator. Each operator has a unique key
# whose SHA-256 fingerprint is stored in governance/identities.json.
# Generate a new key:  python scripts/keygen.py human:alice
# Default dev key:     test-key-change-me  (pre-hashed in identities.json)

# ─── PostgreSQL (docker-compose) ──────────────────────────────
POSTGRES_USER=admin
POSTGRES_PASSWORD=password123
POSTGRES_DB=governance_control_plane

# ─── Database URL (used by gateway + dashboard) ──────────────
DATABASE_URL=postgresql://admin:password123@localhost:5433/governance_control_plane

# ─── Dashboard (ui/.env.local) ────────────────────────────────
# GATEWAY_URL=http://localhost:8000

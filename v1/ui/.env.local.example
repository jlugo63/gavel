# Gavel — Database Connection
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
DATABASE_URL=postgresql://admin:password123@localhost:5433/governance_control_plane

# Governance Gateway (for human approval proxying)
GATEWAY_URL=http://localhost:8000

# Per-operator API key. Each operator has their own key whose SHA-256
# fingerprint is stored in governance/identities.json.
# Default dev key: test-key-change-me
HUMAN_API_KEY=test-key-change-me
